#include<bits/stdc++.h>
using namespace std;

void solve()
{
	int n;
	while(cin >> n)
	{
		vector<int> a;
		vector<int> diff(n + 1);
	}
}

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	solve();
	return 0;
}
